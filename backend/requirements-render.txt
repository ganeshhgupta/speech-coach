# Same as requirements.txt minus torch/pyannote/faster-whisper — used for the
# Render Docker image to keep it small and the instance cheap. "Two speakers"
# diarization mode isn't available on Render as a result; it degrades
# gracefully to the same "speaker detection failed, continuing without it"
# path used locally when HUGGINGFACE_TOKEN isn't set (that var IS set here,
# but only for SC_TRANSCRIBE_BACKEND=hf, not diarization). faster-whisper is
# dropped too: transcription runs via the Hugging Face Inference API instead
# of locally (see pipeline/transcribe.py) — the free-tier instance doesn't
# have the RAM/CPU to run Whisper in-process without OOM-crashing.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
praat-parselmouth==0.4.5
numpy==2.4.6
soundfile==0.12.1
av==13.1.0
requests==2.32.3
anthropic==1.8.0
python-dotenv==1.2.3
psycopg[binary]==3.3.6
boto3==1.43.101
PyJWT==2.15.0
cryptography==50.0.1
