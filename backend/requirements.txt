fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
faster-whisper==1.1.1
praat-parselmouth==0.4.5
numpy==2.4.6
soundfile==0.12.1
av==13.1.0
requests==2.32.3
anthropic==1.8.0
python-dotenv==1.2.3
psycopg[binary]==3.3.6
boto3==1.43.101
PyJWT==2.15.0
cryptography==50.0.1

# Optional: only needed for "two speakers" diarization mode. Skip these two
# (and don't set HUGGINGFACE_TOKEN) to keep the install light; single-speaker
# analysis works without them.
torch==2.14.0+cpu
pyannote.audio==4.0.7
